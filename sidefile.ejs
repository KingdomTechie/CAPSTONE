<h6>Required Skills</h6>
                        <div class="btn-group">
                            <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                            Required Skills
                            </button>
                            <ul class="dropdown-menu">
                            <% for (j=0; j < joblistings[i].requiredSkills.length; j++){ %>
                                    <li><a class="dropdown-item" href="#"><%=joblistings[i].requiredSkills[j]%></a></li>
                                <%  } %>
                            </ul>
                        </div>


    <h6>Required Skills</h6>
            <div class="btn-group">
                <button type="button" class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                Required Skills
                </button>
                <ul class="dropdown-menu">
                    <% joblistings.forEach(job =>  { %>
                        <li class="dropdown-item"><%=job.requiredSkills%></li>
                    <%  }) %>
                </ul>
            </div>